<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:LoadingExemple"
             x:Class="LoadingExemple.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="Exemplo de Loading" 
           HorizontalOptions="Center"
           VerticalOptions="Start" />

        <Button x:Name="btnLoading" Clicked="BtnLoading_Clicked" Text="LoadingExemple" TextColor="White" BackgroundColor="#3b5998" Margin="10,30,10,30" />

        <ActivityIndicator x:Name="actIndLoading" 
                           IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           Color="Blue" />
    </StackLayout>

</ContentPage>
